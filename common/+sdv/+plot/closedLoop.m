function figureHandle = closedLoop(results, scenario, environmentName)
%CLOSEDLOOP Create the standard closed-loop result figure.

figureHandle = plot_phase0_results( ...
    results, scenario, environmentName);

end
